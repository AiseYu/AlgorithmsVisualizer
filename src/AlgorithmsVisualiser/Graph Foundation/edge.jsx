import React, { useState } from 'react';
import Node from './node';
function Edge(){
    const [nodes , setNodes] = useState({origin: Node.id, next: Node.id});
    const [directed , setDirected] = useState(false);
    const [weight , setWeight] = useState(1);
    
    


    return(
        <>
        
        </>

    );
}

export default Edge;